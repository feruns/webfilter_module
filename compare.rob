Feature: Compare multiple XML tag values with references and apply delta threshold

  Scenario: Compare multiple tag values between test and reference XML files
    Given a reference XML file "reference_data.xml"
    And a test XML file "test_data.xml"
    When I extract the values for tags "a/b/c/d" and "a/b/c/e"
    Then the test should pass if all deltas are within 2 standard deviations
